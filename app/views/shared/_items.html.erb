<%= render(ItemUserComponent.new) if Current.user&.super_admin? %>
<%= render(ItemBeneficiaryComponent.new) if Current.user&.super_admin? || Current.user&.admin? %>
<%= render(ItemDeliveryComponent.new) if Current.user %>
<%= render(ItemQueryComponent.new) if Current.user&.super_admin? || Current.user&.admin? %>

<!-- <%= link_to t('.login'), new_session_path, class: "nav-link" unless Current.user %> -->
<%= link_to t('.logout'), session_path(Current.user.id), data: {
  turbo_method: :delete,
  turbo_confirm: t('common.confirm')
}, class: "nav-link" if Current.user %>
